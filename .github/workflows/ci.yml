name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

env:
  GO_VERSION: "1.26"

jobs:
  # --- 1) Lint + derleme --------------------------------------------------
  lint-build:
    name: Lint & derleme (${{ matrix.module }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: [producer, consumer, webviz]
    defaults:
      run:
        working-directory: ${{ matrix.module }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: ${{ matrix.module }}/go.sum

      - name: gofmt kontrolü
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "Bu dosyalar gofmt'lenmemiş:"
            echo "$unformatted"
            exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: Derleme
        run: go build ./...

  # --- 1b) Sır hijyeni ----------------------------------------------------
  #
  # secrets/ altındaki dosyaların gerçekten şifreli olduğunu doğrular.
  # SOPS'un asıl kırılganlığı şifrelemenin kendisi değil, bir dosyanın
  # şifrelenmeyi UNUTULMASIDIR — bu iş tek bir `git add` ile sessizce ters
  # gidebilir. Bu koruma onu yayına çıkmadan yakalar.
  secrets-hygiene:
    name: Sır hijyeni
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Şifrelenmemiş sır var mı
        run: ./scripts/secrets.sh check

      - name: age özel anahtarı sızmış mı
        run: |
          # .gitignore koruma sağlar ama `git add -f` onu atlar; bu son savunma.
          #
          # Kalıp anahtarın BİÇİMİNE bakıyor, sadece "AGE-SECRET-KEY" dizesine
          # değil: gerçek bir age özel anahtarı bu önekten sonra ~59 karakter
          # daha taşır. Salt önek araması, dokümantasyondaki ve Terraform
          # validation'ındaki meşru referansları da yakalayıp yanlış alarm
          # üretiyordu — sürekli yanlış alarm veren bir koruma, kapatılan bir
          # korumadır.
          if git grep -nIE "AGE-SECRET-KEY-1[A-Z0-9]{50,}" -- .; then
            echo "HATA: depoda age ÖZEL anahtarı bulundu!"
            exit 1
          fi
          echo "age özel anahtarı sızıntısı yok."

  # --- 2) Unit testler ----------------------------------------------------
  test:
    name: Test (${{ matrix.module }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: [producer, consumer, webviz]
    defaults:
      run:
        working-directory: ${{ matrix.module }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: ${{ matrix.module }}/go.sum

      # -race yarış koşullarını yakalar: producer 8 goroutine ile paralel
      # üretiyor, consumer zone başına paralel yazıyor, webviz eşzamanlı
      # istemcilere yayın yapıyor - üçü de yarışa açık kod.
      - name: go test -race
        run: go test ./... -race -cover

  # --- 3) Uçtan uca boru hattı testi --------------------------------------
  integration:
    name: Uçtan uca boru hattı
    runs-on: ubuntu-latest
    needs: [lint-build, test]
    steps:
      - uses: actions/checkout@v7

      - name: Stack'i düşük yükle ayağa kaldır
        run: docker compose -f docker-compose.yml -f deploy/docker-compose.ci.yml up -d --build

      - name: Prometheus hazır olana kadar bekle
        run: |
          for i in $(seq 1 60); do
            if curl -sf http://localhost:9090/-/ready >/dev/null 2>&1; then
              echo "Prometheus hazır (${i}. deneme)"
              exit 0
            fi
            sleep 5
          done
          echo "Prometheus 5 dakikada hazır olmadı"
          exit 1

      - name: Boru hattının veri işlediğini doğrula
        run: |
          # Metriklerin birikmesi için bir tur bekle
          sleep 45

          q() {
            curl -s "http://localhost:9090/api/v1/query?query=$1" \
              | jq -r '.data.result[0].value[1] // "0"'
          }

          produced=$(q pulsecity_producer_messages_total)
          processed=$(q pulsecity_consumer_messages_processed_total)
          dlq=$(q pulsecity_consumer_dlq_total)
          errors=$(q pulsecity_producer_errors_total)

          echo "Üretilen : $produced"
          echo "İşlenen  : $processed"
          echo "DLQ      : $dlq"
          echo "Hata     : $errors"

          [ "${produced%.*}" -gt 0 ]  || { echo "HATA: producer hiç mesaj üretmedi"; exit 1; }
          [ "${processed%.*}" -gt 0 ] || { echo "HATA: consumer hiç mesaj işlemedi"; exit 1; }
          [ "${dlq%.*}" -eq 0 ]       || { echo "HATA: DLQ'ya mesaj düştü ($dlq)"; exit 1; }
          [ "${errors%.*}" -eq 0 ]    || { echo "HATA: producer yazma hatası aldı ($errors)"; exit 1; }

      - name: ScyllaDB'ye gerçekten yazıldığını doğrula
        run: |
          rows=$(docker exec pulsecity-scylla cqlsh --request-timeout=120 \
            -e "SELECT COUNT(*) FROM pulsecity.vehicle_pings;" \
            | grep -Eo '^\s+[0-9]+\s*$' | tr -d ' ')
          echo "ScyllaDB satır sayısı: ${rows:-0}"
          [ "${rows:-0}" -gt 0 ] || { echo "HATA: ScyllaDB'ye hiç satır yazılmadı"; exit 1; }

      - name: Faz 8 anomali metriklerinin yayınlandığını doğrula
        run: |
          zones=$(curl -s http://localhost:9090/api/v1/label/zone_id/values | jq '.data | length')
          echo "Bölge sayısı: $zones"
          [ "$zones" -eq 9 ] || { echo "HATA: 9 bölge bekleniyordu, $zones geldi"; exit 1; }

          series=$(curl -s "http://localhost:9090/api/v1/query?query=pulsecity_zone_speed_baseline_kmh" \
            | jq '.data.result | length')
          echo "Baseline serisi: $series"
          [ "$series" -gt 0 ] || { echo "HATA: Faz 8 baseline metriği üretilmedi"; exit 1; }

      - name: Faz 9 haritasının yanıt verdiğini doğrula
        run: |
          code=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/)
          [ "$code" = "200" ] || { echo "HATA: harita sayfası HTTP $code döndü"; exit 1; }

          consumed=$(curl -s http://localhost:8080/metrics \
            | awk '/^pulsecity_webviz_messages_consumed_total/ {print $2}')
          echo "webviz tükettiği mesaj: ${consumed:-0}"
          [ "${consumed%.*}" -gt 0 ] || { echo "HATA: webviz Kafka'dan mesaj okumadı"; exit 1; }

      - name: Hata durumunda logları göster
        if: failure()
        run: docker compose -f docker-compose.yml -f deploy/docker-compose.ci.yml logs --tail 120

      - name: Temizlik
        if: always()
        run: docker compose -f docker-compose.yml -f deploy/docker-compose.ci.yml down -v

  # --- 4) Tedarik zinciri: güvenlik taraması + SBOM ------------------------
  #
  # Bu job yayınlanacak imajın TAM OLARAK kendisini tarar: aynı Dockerfile,
  # aynı digest'e sabitlenmiş base imaj. Sadece kaynak kodu taramak yetmez -
  # açıkların çoğu uygulama kodunda değil, base imajın sistem paketlerinde olur.
  #
  # Neden `ignore-unfixed: true`: upstream'de henüz yaması olmayan bir CVE için
  # build'i kırmak, yapılabilecek bir şey olmadığı halde boru hattını durdurur
  # ve zamanla "kırmızıyı görmezden gelme" alışkanlığı yaratır. Yaması olan
  # HIGH/CRITICAL ise gerçekten aksiyon alınabilir bir bulgudur - orada kırıyoruz.
  security:
    name: Güvenlik taraması & SBOM (${{ matrix.module }})
    runs-on: ubuntu-latest
    needs: [lint-build]
    permissions:
      contents: read
      security-events: write   # SARIF'i Security sekmesine yüklemek için
    strategy:
      fail-fast: false
      matrix:
        module: [producer, consumer, webviz]
    steps:
      - uses: actions/checkout@v7

      # Taranacak imajı yerelde üret (push etmeden). docker-push job'ı aynı
      # context'ten aynı imajı üretir; burada tarananla orada yayınlanan aynıdır.
      - name: İmajı build et (yerel)
        run: docker build -t pulsecity-${{ matrix.module }}:scan ${{ matrix.module }}

      - name: Trivy — imaj açık taraması
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: pulsecity-${{ matrix.module }}:scan
          format: table
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: "1"        # yaması olan HIGH/CRITICAL varsa build kırılır

      # Aynı taramanın SARIF çıktısı GitHub Security sekmesine yüklenir.
      # exit-code YOK: burada amaç raporlamak, kırma kararını yukarıdaki adım verdi.
      - name: Trivy — SARIF üret
        if: always()
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: pulsecity-${{ matrix.module }}:scan
          format: sarif
          output: trivy-${{ matrix.module }}.sarif
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          # Trivy, SARIF ciktisinda `severity` filtresini VARSAYILAN OLARAK
          # uygulamaz - bu bayrak olmadan Security sekmesine LOW/MEDIUM dahil
          # her sey duser ve yukaridaki kapiyla tutarsiz bir liste olusur.
          limit-severities-for-sarif: true

      - name: SARIF'i Security sekmesine yükle
        if: always()
        # Private repoda GitHub Advanced Security olmadan bu adım yetkisizlik
        # verir; raporlama eksikliği CI'ı kırmamalı.
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy-${{ matrix.module }}.sarif
          category: trivy-${{ matrix.module }}

      # Dockerfile yanlış yapılandırmaları (root kullanıcı, sabitlenmemiş base
      # imaj, gereksiz ayrıcalık). Faz 13'te düzeltilen sınıfın regresyon kilidi.
      - name: Trivy — Dockerfile yapılandırma denetimi
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: ${{ matrix.module }}
          format: table
          severity: CRITICAL,HIGH
          exit-code: "1"

      # SBOM (Software Bill of Materials): imajın içindeki her paketin makine
      # okunur envanteri. Yeni bir CVE duyurulduğunda "bu imaj etkileniyor mu"
      # sorusu imajı yeniden taramadan, SBOM üzerinden yanıtlanabilir.
      - name: SBOM üret (SPDX)
        uses: anchore/sbom-action@v0
        with:
          image: pulsecity-${{ matrix.module }}:scan
          format: spdx-json
          output-file: sbom-${{ matrix.module }}.spdx.json

      - name: SBOM'u artifact olarak sakla
        uses: actions/upload-artifact@v7
        with:
          name: sbom-${{ matrix.module }}
          path: sbom-${{ matrix.module }}.spdx.json
          retention-days: 90

  # --- 5) IaC: Terraform doğrulama + güvenlik taraması ---------------------
  #
  # `apply` YOK ve bilinçli olarak yok: bu iş akışının bulut kimlik bilgisi
  # tutması gerekmez. Buradaki amaç, altyapı kodunun biçimli, geçerli ve
  # yanlış yapılandırma içermediğini her push'ta kanıtlamak.
  #
  # `terraform plan` da yok — plan gerçek state ve kimlik bilgisi ister;
  # onu çalıştıran taraf operatörün kendisi (bkz. infra/terraform/README.md).
  iac:
    name: Terraform doğrula & tara
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    defaults:
      run:
        working-directory: infra/terraform
    steps:
      - uses: actions/checkout@v7

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: terraform fmt
        run: terraform fmt -check -recursive -diff

      # -backend=false: state arka ucunu başlatmadan yalnızca provider'ları
      # indirir. validate için yeterli, kimlik bilgisi gerektirmez.
      - name: terraform init
        run: terraform init -backend=false -input=false

      - name: terraform validate
        run: terraform validate -no-color

      # Yanlış yapılandırma taraması: açık security group, şifresiz disk,
      # IMDSv1, aşırı geniş IAM politikası gibi sınıflar.
      #
      # Kabul edilmiş istisnalar kodda `# trivy:ignore:<ID>` ile ve GEREKÇESİYLE
      # işaretlenir (bkz. security.tf AWS-0104). Bir istisnayı sessizce
      # bastırmakla, yazılı olarak kabul etmek arasındaki fark bu.
      - name: Trivy — Terraform yapılandırma denetimi
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: infra/terraform
          format: table
          severity: CRITICAL,HIGH,MEDIUM
          exit-code: "1"

      - name: Trivy — SARIF üret
        if: always()
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: infra/terraform
          format: sarif
          output: trivy-terraform.sarif
          severity: CRITICAL,HIGH,MEDIUM
          limit-severities-for-sarif: true

      - name: SARIF'i Security sekmesine yükle
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: infra/terraform/trivy-terraform.sarif
          category: trivy-terraform

  # --- 6) İmajları GHCR'a gönder ------------------------------------------
  docker-push:
    name: İmaj yayınla (${{ matrix.module }})
    runs-on: ubuntu-latest
    # Güvenlik taraması geçmeden imaj yayınlanmaz: taramanın yayın yolunda
    # KAPI olması, sadece rapor üretmesinden farklıdır.
    needs: [integration, security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        module: [producer, consumer, webviz]
    steps:
      - uses: actions/checkout@v7

      # GHCR imaj adları küçük harf olmak zorunda; depo sahibi adı büyük harf
      # içerebildiği için dönüştürülüyor.
      - name: Depo sahibini küçük harfe çevir
        id: owner
        run: echo "lc=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          # Ek secret gerekmiyor: GITHUB_TOKEN paket yazma yetkisiyle geliyor.
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: İmaj etiketlerini hazırla
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ steps.owner.outputs.lc }}/pulsecity-${{ matrix.module }}
          tags: |
            type=raw,value=latest
            type=sha,format=long

      - uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.module }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # --- 7) VPS'e dağıtım (henüz pasif) -------------------------------------
  #
  # Canlıya alma bilinçli olarak sonraya bırakıldı. Bu job iki koşul birden
  # saglanmadan ÇALIŞMAZ:
  #   1. Workflow elle tetiklenmiş olmalı (Actions -> Run workflow)
  #   2. Depoda DEPLOY_ENABLED adlı repository variable "true" olmalı
  #      (Settings -> Secrets and variables -> Actions -> Variables)
  #
  # Ayrıca şu secret'lar tanımlanmalı:
  #   VPS_HOST     - sunucunun IP adresi veya alan adı
  #   VPS_USER     - SSH kullanıcısı (docker grubunda olmalı)
  #   VPS_SSH_KEY  - private key (passphrase'siz)
  #
  # Detaylı sunucu hazırlığı için: deploy/DEPLOY.md
  deploy:
    name: VPS'e dağıt (manuel)
    runs-on: ubuntu-latest
    needs: [integration]
    if: github.event_name == 'workflow_dispatch' && vars.DEPLOY_ENABLED == 'true'
    steps:
      - uses: actions/checkout@v7

      - name: Sunucuda güncelle ve yeniden başlat
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd ~/pulsecity
            git pull --ff-only
            docker compose -f docker-compose.yml -f deploy/docker-compose.prod.yml up -d --build
            docker compose ps
